\c prueba

CREATE TABLE cliente(
    id_cliente INT PRIMARY KEY,
    name_cliente VARCHAR(30) NOT NULL,
    rut_cliente VARCHAR NOT NULL,
    address_cliente VARCHAR(30) NOT NULL
);


--El cliente tiene id, nombre, rut y dirección--